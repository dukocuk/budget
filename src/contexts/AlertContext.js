/**
 * Alert Context - Centralized alert state
 * Follows the same pattern as ModalContext and ExpenseContext
 */

import { createContext } from 'react';

export const AlertContext = createContext(null);
